"""Period 8 AI workflow foundation (US-027).

Shared building blocks every AI feature runs on: a provider abstraction
(Gemini primary, deterministic fallback), the ``BaseAIWorkflow`` standard flow,
a typed error taxonomy, and a redacting logger. See
``docs/decisions/0012-ai-workflow-standards.md``.
"""
